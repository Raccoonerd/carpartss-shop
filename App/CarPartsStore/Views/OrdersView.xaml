<UserControl x:Class="CarPartsStore.Views.OrdersView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:vm="clr-namespace:CarPartsStore.ViewModels">

    <UserControl.DataContext>
        <vm:OrdersViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style x:Key="HeaderStyle" TargetType="DataGridColumnHeader"
               BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
            <Setter Property="Background" Value="{DynamicResource PrimaryHueDarkBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryHueDarkForegroundBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="44"/>
        </Style>
        <Style x:Key="RowStyle" TargetType="DataGridRow"
               BasedOn="{StaticResource MaterialDesignDataGridRow}">
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryHueLightBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SecondaryHueMidBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource SecondaryHueMidForegroundBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CellStyleX" TargetType="DataGridCell"
               BasedOn="{StaticResource MaterialDesignDataGridCell}">
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SecondaryHueMidBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource SecondaryHueMidForegroundBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource SecondaryHueMidBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="3"
                   Text="Historia zamówień"
                   FontSize="20" FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <materialDesign:Card Grid.Row="1" Grid.ColumnSpan="3" Padding="12" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" Margin="0,0,12,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ComboBox Grid.Column="0"
                              materialDesign:HintAssist.Hint="Filtruj po kliencie"
                              Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                              ItemsSource="{Binding Customers}"
                              DisplayMemberPath="CompanyName"
                              SelectedItem="{Binding FilterCustomer, Mode=TwoWay}"/>

                    <Button Grid.Column="1"
                            Command="{Binding ClearCustomerFilterCommand}"
                            Style="{StaticResource MaterialDesignIconButton}"
                            ToolTip="Wyczyść filtr klienta"
                            VerticalAlignment="Bottom"
                            Margin="4,0,0,0">
                        <materialDesign:PackIcon Kind="Close"/>
                    </Button>
                </Grid>

                <DatePicker Grid.Column="1"
                            materialDesign:HintAssist.Hint="Od daty"
                            Style="{StaticResource MaterialDesignFloatingHintDatePicker}"
                            SelectedDate="{Binding FilterDateFrom, Mode=TwoWay}"
                            Margin="0,0,12,0"/>

                <DatePicker Grid.Column="2"
                            materialDesign:HintAssist.Hint="Do daty"
                            Style="{StaticResource MaterialDesignFloatingHintDatePicker}"
                            SelectedDate="{Binding FilterDateTo, Mode=TwoWay}"
                            Margin="0,0,12,0"/>

                <Button Grid.Column="3"
                        Command="{Binding ClearFiltersCommand}"
                        Style="{StaticResource MaterialDesignFlatDarkBgButton}"
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="FilterRemove" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="WYCZYŚĆ FILTRY"/>
                    </StackPanel>
                </Button>
            </Grid>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="2" Grid.Column="0">
            <DataGrid ItemsSource="{Binding Orders}"
                      SelectedItem="{Binding SelectedOrder, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      RowBackground="{DynamicResource MaterialDesignPaper}"
                      AlternatingRowBackground="{DynamicResource MaterialDesignChipBackground}"
                      Style="{StaticResource MaterialDesignDataGrid}"
                      ColumnHeaderStyle="{StaticResource HeaderStyle}"
                      RowStyle="{StaticResource RowStyle}"
                      CellStyle="{StaticResource CellStyleX}"
                      materialDesign:DataGridAssist.CellPadding="13 8 8 8"
                      materialDesign:DataGridAssist.ColumnHeaderPadding="8">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nr" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Data" Binding="{Binding OrderDate, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="140"/>
                    <DataGridTextColumn Header="Klient" Binding="{Binding Customer.CompanyName}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>

        <Grid Grid.Row="2" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <materialDesign:Card Grid.Row="0">
                <DataGrid ItemsSource="{Binding SelectedOrderItems}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          RowBackground="{DynamicResource MaterialDesignPaper}"
                          AlternatingRowBackground="{DynamicResource MaterialDesignChipBackground}"
                          Style="{StaticResource MaterialDesignDataGrid}"
                          ColumnHeaderStyle="{StaticResource HeaderStyle}"
                          RowStyle="{StaticResource RowStyle}"
                          CellStyle="{StaticResource CellStyleX}"
                          materialDesign:DataGridAssist.CellPadding="13 8 8 8"
                          materialDesign:DataGridAssist.ColumnHeaderPadding="8">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Część" Binding="{Binding Part.Name}" Width="*"/>
                        <DataGridTextColumn Header="Nr katalog." Binding="{Binding Part.CatalogNumber}" Width="120"/>
                        <DataGridTextColumn Header="Ilość" Binding="{Binding Quantity}" Width="70"/>
                        <DataGridTextColumn Header="Cena" Binding="{Binding UnitPrice, StringFormat={}{0:N2} zł}" Width="90"/>
                    </DataGrid.Columns>
                </DataGrid>
            </materialDesign:Card>

            <materialDesign:Card Grid.Row="1" Padding="16" Margin="0,10,0,0">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Pozycji: " FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding SelectedOrderItemsCount}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Suma zamówienia: "
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
                        <TextBlock Text="{Binding SelectedOrderTotal, StringFormat={}{0:N2} zł}"
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   Foreground="{DynamicResource SecondaryHueMidBrush}"/>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>
